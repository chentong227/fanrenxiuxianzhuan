---
description: 考据红线与档案引导（接手必读 AGENTS.md）
alwaysApply: true
---

# 考据红线（最高优先级）

1. 接手任务先读根目录 `AGENTS.md`（档案地图+方法论+工程惯例），再读其列出的必读档案。
2. 剧情/人物/物品/手段的时间线与获取方式：**动漫版为唯一锚点**，小说仅补细节；
   先 grep `docs/` 既有调研，档案没有的必须重新搜索并 ≥2 独立来源交叉核定。
   **绝不凭模型记忆写设定**——历史上多次因此全盘返工（见 AGENTS.md 事故清单）。
3. 设计先落档（docs/）、与用户沟通拍板后再实装；不要边设计边改代码。
4. 改 `js/state.js` 必须同步 `_migrate()`；改战斗/数值必跑 `test/` 蒙特卡洛脚本；
   发版用 `node scripts/bump.js`。PowerShell 下命令链用 `;` 不用 `&&`。
