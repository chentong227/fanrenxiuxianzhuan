# 战场纵深完全案：透视地台 + 高度带（2.5D 的"地"与"天"）

> 状态：**D0~D3 已实装（v85，2026-06-12 用户拍板"做吧，做完返修"）；D4=预案待乱星海**。
> 实装落点：地台与排地线=css `.axis-field` 变量族（--ground0/--lane-gap/--lanes，JS 注入排数）；
> 接触影/光柱=`.axis-unit::after`/`.air-pillar`（--ay 自动归地）；高度档=`.air-1/2/3`；
> 镜头三联动=ui.js renderCombat（uscale/V/skyShift 挂 airGrade）+ combat.js moveCap
> （airGrade→airMove，canMoveTo/playerMove/movableCells/敌方步程全链路）。
> 测试：combat.test.js §16（凌空机动断言已更新）。验收截图：v85_air_depth.png
> （黑框消失/身法×2/高度与僚位分层/影留地线）。
> 结论先行：**纵深地基先于升空缩放**——"脚踩不踩地"是地基的副产品，
> 地基不打，升空怎么调都飘。
> 关联：`ally-ai-design.md` 八节（2.5 排制）、`flight-ladder-design.md`（境界分档）、
> `explore-redesign.md` 四（镜头语法）、AGENTS 当前状态指针。
> 用户裁决锚点：①升空=视野+可移动范围同步增大、人物按景深缩小、**地面单位仍踩地**；
> ②高度/视野/机动**全挂境界**（"韩立飞得比南陇侯高"=实力的俯视）；
> ③元婴大战可飞出屏幕（切超远景演出，"会很帅"）；④v78 前景暗带 fg-band 已否——
> 纵深不靠贴片；⑤v85 整场 transform:scale 已否——缩场=单位层悬空+黑框。

---

## 〇·五、觅长生对照诊断（2026-06-12 用户问"为什么做不到"）

觅长生战斗图的踩地感来自三件事，按权重排：
1. **底图是"竞技场"不是"风景"**（权重最大）：画面底部 40% 是一块按人物比例
   画出来的平整近地（土路/草坪，纹理是脚边尺度），人站上去天然就实。我们的
   bt_*/pano 底图是远眺风景——脚下那块在画里是几公里外的山谷，比例对不上，
   CSS 画再多地线也是浮在风景上。**根治=按"竞技场构图"重生成战斗底图**
   （genart prompts v2：底部1/3近地、脚边尺度纹理、低机位微俯、站位带无遮挡）。
2. **选中圈在正脚下**：觅长生的选中圈是贴地透视椭圆=站位标记。我们旧圈位置
   偏移且弱——已修（target/self 脚圈贴接触影、随 --ay 凌空归地）。
3. **接触影+色调融合**：脚下椭圆影 + 人物色调贴场景。影已有；色调融合待
   底图换竞技场后再调（D1 返修轮）。

## 〇、现状诊断（为什么"脚不踩地"）

1. **地面是一条线，不是一个面**：所有单位 `bottom:0` 锚同一条底线；排（lane）靠
   `translate` 上移硬挪——挪上去之后脚下没有"那一排的地面"，自然像飘。
2. **背景透视与站位不咬合**：底图有地面近大远小的透视，单位排布却是纯平视横排，
   底图的消失线与单位脚位对不上。
3. **高度与深度共用同一种手段**（都是往上挪 px）：lane-3 上移 44px 与 airborne
   上移 64px 在画面上几乎是一回事——"远"和"高"分不开。
4. **缩放缺透视锚**：人物缩小但脚位不动 → 读成"变小了"，不是"远了/高了"。

## 一、目标模型：透视地台（地）+ 高度带（天）

### 地：透视地台（fake-3D ground plane）
- 战场下部画一块**向远处收窄的透视地台**（纯 CSS 渐变/边线，零贴图成本）：
  近缘=战位排地线，远缘=最深僚位排地线，消失感由两条收拢的侧缘线+横向地纹给出。
- **每排有自己的地线**：`laneY(k)`、`laneScale(k)`、阴影宽度/浓度——
  一族 CSS 变量（`--depth-k`）统一公式化；off-row 并入（=半档深度），
  不再各管各的魔法数。
- **单位脚锚到所在排的地线**，接触阴影贴着那条地线压扁——
  "踩在地上"的真相是：**每一排都有地**。
- **背景咬合**：底图地面带与地台范围对齐（锚点/暗角微调）；pano 长卷沿用
  半速视差。倾角克制（视觉等效 5~8°），避免与平视立绘打架。

### 天：高度带（与"远"彻底分家）
- airborne 单位**离开地台**进入天空带，三件套缺一不可：
  **遁光踏足**（已做）+ **地面投影留在原格地线上**（影在地、人在天）+
  淡淡的垂直光柱把两者连起来（高度的视觉脐带）。
- **高度=境界档**（airGrade，字段已落）：
  | 档 | 对应 | 升空高度 | 凌空身法 | 镜头 |
  |---|---|---|---|---|
  | 1 | 筑基·御器 | 地台上缘+60px | +2 | 视野 V 11→13 |
  | 2 | 结丹·御空 | 天空带中部 | +3 | V 11→15 |
  | 3 | 元婴·遁光 | 画框上缘 | +4 | V 15+，uscale 加深 |
  档位差同屏可见：你档 2 它档 1——你俯视它（"飞得比南陇侯高"的机制化）。
- **远景演出（D4 预案，乱星海后实装）**：双方 airGrade≥3 且皆空
  → 切"超远景模式"：背景换远景图层（或底图大幅缩小+云层前景），
  单位化作小剪影+拉长光效——元婴大战飞出屏幕。

### 镜头：一条公式管三联动
- 升空 = `uscale`（人物景深缩）+ `V`（视野格数）+ `moveCap`（可走格数）
  三者同源于 airGrade——"镜头后拉"是因，三者是果。
- 地面单位在镜头后拉时只随 uscale 缩、**脚仍锚地线**（地台不缩，地线不动）。

## 二、实装切片（待拍板）

| 序 | 内容 | 验收 |
|---|---|---|
| D0 | 透视地台底座：CSS 地台（渐变+侧缘收拢线）；laneY/laneScale/阴影公式化（--depth-k 变量族），off-row 并入 | 站着不动就能看出三排前后；脚有"地"可踩 |
| D1 | 背景咬合：底图锚点/暗角与地台对齐；旷野/洞窟 pano 两图不穿帮 | 换图不破纵深 |
| D2 | 高度带重做：airGrade 三档高度+投影留地+光柱；与地台联动 | 筑基/元婴档肉眼可分；"高"≠"远" |
| D3 | 镜头三联动收尾：uscale/V/moveCap 挂 airGrade；**接完已停手的 playerMove 链路**（capOverride/movableCells 剩余步数） | 升空=视野+步数+景深齐动、无黑框、脚不飘 |
| D4 | 超远景演出预案（元婴空战）——只入档不实装 | —— |

## 三、风险与对策

| 风险 | 对策 |
|---|---|
| 透视地台与平视立绘打架 | 倾角克制 5~8° 视觉等效；地台只给"线索"不给强透视 |
| 手机性能 | 纯 CSS 渐变+变量，零图层成本；不加 DOM |
| 排数多时地台高度不够 | 地台高度=f(排数)；4 排大战场配 D3 镜头后拉 |
| 半成品口子（v85） | moveCap 已生效但 playerMove 剩余步数校验未接 capOverride——空中可能多走（D3 修） |
